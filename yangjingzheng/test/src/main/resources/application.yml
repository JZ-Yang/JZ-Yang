# General configuration
server:
  port: 12348
  servlet:
    context-path: /examples/mysql

logging:
  config: classpath:log4j.properties

spring:
   main:    allow-bean-definition-overriding: true
   datasource:
     url: jdbc:mysql://127.0.0.1:3306/test
     username: root
     password: root
     driverClassName: com.mysql.cj.jdbc.Driver
